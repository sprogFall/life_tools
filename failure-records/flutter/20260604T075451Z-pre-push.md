# pre-push 失败记录 - flutter - 20260604T075451Z

状态: 已归纳
阶段: pre-push
模块: flutter
退出码/结论: 1
记录时间(UTC): 20260604T075451Z
分支: main
提交: 6c0adad

## 失败命令
```bash
/opt/flutter/bin/flutter analyze 
```

## 关联改动文件
- lib/tools/work_photo/pages/work_photo_project_detail_page.dart
- lib/tools/work_photo/widgets/work_photo_asset_grid.dart
- test/tools/work_photo/work_photo_project_detail_page_test.dart

## 精简错误信息
- `flutter analyze` 报 `prefer_initializing_formals`：`_ProjectDetailTreeNode.item` 构造函数使用与字段同名的普通参数再赋值给 `item` 字段。
- 影响范围仅为外拍助手项目详情页新增的私有树节点辅助类。

## 解决方案
- 将构造函数参数改为 `projectItem`，避免同名参数触发 `prefer_initializing_formals`。
- 修改文件：`lib/tools/work_photo/pages/work_photo_project_detail_page.dart`。
- 修复后重新执行：`bash scripts/pre-push.sh --scope flutter --test-module work_photo`。

## 预防方案
- 新增私有数据结构时，若构造参数只是直接赋给同名字段，使用 initializing formal；若还要用该参数派生其他字段，参数名避免与字段同名。
- 提交前继续以 `pre-push` 的 `flutter analyze` 为准，不只运行模块测试。

## 原始失败信息
```text
Flutter assets will be downloaded from https://storage.flutter-io.cn. Make sure you trust this source!
Analyzing life_tools...                                         

   info • Use an initializing formal to assign a parameter to a field • lib/tools/work_photo/pages/work_photo_project_detail_page.dart:465:7 • prefer_initializing_formals

1 issue found. (ran in 37.3s)
```
