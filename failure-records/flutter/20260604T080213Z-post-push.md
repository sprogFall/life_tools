# post-push 失败记录 - flutter - 20260604T080213Z

状态: 已归纳
阶段: post-push
模块: flutter
退出码/结论: max_polls_exceeded
记录时间(UTC): 20260604T080213Z
分支: main
提交: 5660f5d

## 失败命令
```bash
post-push monitor
```

## 关联改动文件
- failure-records/flutter/20260604T075451Z-pre-push.md
- lib/tools/work_photo/pages/work_photo_project_detail_page.dart
- lib/tools/work_photo/widgets/work_photo_asset_grid.dart
- test/tools/work_photo/work_photo_project_detail_page_test.dart

## 精简错误信息
- `post-push.sh --max-polls 3` 未能查询到 GitHub Actions 状态，达到最大轮询次数后退出。
- 运行日志中的直接错误为 `curl: (7) Failed to connect to 192.168.31.207 port 20171`，本机代理不可达导致无法访问 GitHub API。
- 代码提交 `5660f5d` 已成功 push 到 `origin/main`，该记录不表示远端 workflow 已失败，只表示本地无法完成监控。

## 解决方案
- 代码侧无需修复；需要恢复或取消当前环境的 GitHub 访问代理后重新执行 `bash scripts/post-push.sh --max-polls 3`。
- 本次已完成本地校验：`bash scripts/pre-push.sh --scope flutter --test-module work_photo`。
- 若只补充该失败记录，使用 `docs:` 提交，避免文档记录再次触发 Android 构建监控。

## 预防方案
- 运行 `post-push.sh` 前先确认本地代理或网络出口可访问 GitHub API，尤其是 `192.168.31.207:20171` 这类环境代理。
- 若网络不可用但代码已 push，应在最终说明中明确“远端状态未能监控”，不要误报为构建通过。

## 原始失败信息
```text
达到最大轮询次数 MAX_POLLS=3
attempt=4
max_polls=3
sha=5660f5d9d5f6b336786ac188ad3eb667931c754d
workflow=Build Android APK
```
