# post-push 失败记录 - flutter - 20260601T151203Z

状态: 已归纳
阶段: post-push
模块: flutter
退出码/结论: max_polls_exceeded
记录时间(UTC): 20260601T151203Z
分支: main
提交: e46e80a

## 失败命令
```bash
post-push monitor
```

## 关联改动文件
- android/app/src/main/kotlin/com/example/life_tools/MainActivity.kt
- failure-records/flutter/20260601T145453Z-post-push.md
- lib/tools/work_photo/widgets/work_photo_item_bar.dart

## 精简错误信息
- `post-push.sh --max-polls 6` 达到轮询上限时，远端 `Build Android APK` workflow 仍处于 `in_progress`，脚本按 `max_polls_exceeded` 记录失败。
- 后续通过 GitHub Actions API 查询同一 run `26763398853`，远端最终状态为 `completed / success`，不是构建失败。

## 解决方案
- 无代码修复需求。该记录是本地监控脚本轮询上限过低导致的超时记录。
- 验证方式：查询 `https://api.github.com/repos/sprogFall/life_tools/actions/runs/26763398853`，确认 `status=completed`、`conclusion=success`；查询 jobs 确认 `Flutter Analyze`、`Flutter Test (Shard 0)`、`Flutter Test (Shard 1)`、`Build APK` 均为 `success`。

## 预防方案
- release APK workflow 可能超过 6 次轮询窗口。需要确认远端最终状态时，应增大 `--max-polls`，或在超时后用 Actions run URL/API 二次确认，不要直接按构建失败处理。

## 原始失败信息
```text
达到最大轮询次数 MAX_POLLS=6
attempt=7
max_polls=6
sha=e46e80a2f2768cdf4ed4365a07a00beeeda37025
workflow=Build Android APK
```
