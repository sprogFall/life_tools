# post-push 失败记录 - repo - 20260604T094043Z

状态: 已归纳
阶段: post-push
模块: repo
退出码/结论: failure
记录时间(UTC): 20260604T094043Z
分支: main
提交: 7fa7b7e

## 失败命令
```bash
post-push monitor
```

## 关联改动文件
- .github/workflows/README_build-apk.md
- .github/workflows/build-apk.yml
- android/app/src/main/kotlin/com/example/life_tools/MainActivity.kt
- android/app/src/main/res/xml/update_file_paths.xml
- lib/core/update/app_update.dart
- lib/main.dart
- lib/pages/about_page.dart
- scripts/tests/build-apk_workflow_test.sh
- test/core/update/app_update_service_test.dart
- test/pages/about_page_test.dart

## 精简错误信息
- 远端 GitHub Actions `Build Android APK` 运行失败，run id 为 `26943634477`。
- 失败 job 为 `Flutter Test (Shard 1)`，失败步骤为 `Run shard tests`；`Flutter Analyze` 与 Shard 0 通过，`Build APK` 因前置失败被跳过。
- 本地重新执行同分片 `bash scripts/test_flutter.sh --total-shards 2 --shard-index 1` 完整通过，未复现失败。
- 当前环境 `gh auth status` 未登录，GitHub logs API 返回权限不足，无法获取远端失败步骤的详细日志。

## 解决方案
- 未发现需要修复的业务代码问题；本地同分片验证通过，判断为远端 CI 偶发或远端环境差异导致。
- 补充失败记录，保留 run URL、失败步骤、本地复现命令与复现结果，便于后续同类问题排查。
- 重新提交该失败记录并推送，以触发新的远端 workflow 复核。

验证命令：
```bash
bash scripts/test_flutter.sh --total-shards 2 --shard-index 1
```

## 预防方案
- post-push 发现远端分片失败时，优先在本地执行相同 `scripts/test_flutter.sh --total-shards <n> --shard-index <i>` 复现。
- 若本地无法复现且无法获取远端日志，应在失败记录中明确写入 run URL、失败 job/step、日志权限限制与本地复现结果。
- 需要远端失败详情时，先配置可用的 `GITHUB_TOKEN` 或完成 `gh auth login`，避免只能看到失败步骤而无法定位具体用例。

## 原始失败信息
```text
workflow=Build Android APK
run_id=26943634477
run_url=https://github.com/sprogFall/life_tools/actions/runs/26943634477
status=completed
conclusion=failure

failed_steps:
- step=Run shard tests

failure_logs:
[post-push] 提示: 设置 GITHUB_TOKEN 环境变量可获取失败日志详情
```
